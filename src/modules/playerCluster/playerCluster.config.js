const PlayerClusterConfig = {
    id: 'player.cluster.default',
    chainIds: ['source', 'shell-a', 'dart-a', 'prism', 'compressor', 'blade'],
    energyRatio: 0.78,
    growthCursor: 0,
    heading: -Math.PI * 0.5
};

window.PlayerClusterConfig = PlayerClusterConfig;
