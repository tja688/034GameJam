const PlayerClusterBehavior = {
    id: 'player.cluster.behavior',
    type: 'legacy-player-cluster-adapter',
    entry: 'sceneMovement + sceneCombat + sceneTopology'
};

window.PlayerClusterBehavior = PlayerClusterBehavior;
