const HudMainPresets = {
    default: { ...HudMainConfig },
    mainflow: { id: 'mainflow', compact: false },
    playground: { id: 'playground', compact: true }
};

window.HudMainPresets = HudMainPresets;
